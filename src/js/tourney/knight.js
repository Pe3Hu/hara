//
class knight {
  constructor ( index, side ){
    this.const = {
      index: index,
      side: side
    };
    this.var = {
    };
    this.array = {
    };

    this.init();
  }

  init(){
  }

  draw( offsets ){
  }
}
